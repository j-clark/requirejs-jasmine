src_files:
  # manage these with require

stylesheets:
  - stylesheets/**/*.css

helpers:
  # manage these with require

spec_files:
  # manage these with require

src_dir:

spec_dir:

spec_helper: spec/javascripts/support/jasmine_helper.rb

boot_dir:

boot_files:
